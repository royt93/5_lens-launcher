# Copy this file to keystore.properties and keep the copy outside version control.
# Prefer an absolute path so the signing key can live outside the repository.
storeFile=/absolute/path/to/release-key.jks
storePassword=replace-with-local-secret
keyAlias=replace-with-local-alias
keyPassword=replace-with-local-secret
