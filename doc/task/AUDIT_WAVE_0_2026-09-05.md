# Wave 0 Android release-hardening audit — 2026-09-05

## Scope

This audit covers REL-001 and the local remediation portion of SEC-001. The owner explicitly excluded `store-assets`; all implementation and generated-state changes under that directory were reverted, and SEC-002 was closed by that scope decision.

## Round 1 — 8.6/10

The first combined verification did not qualify for push:

- The complete Pixel instrumentation suite exposed a faulty VIP negative test: the test replaced the configured secret with its supposed invalid key, making the key valid. One of 42 tests failed.
- The signing guard inspected only requested task names. Direct release tasks failed safely, but aggregate `assemble`/`build` task graphs could still produce unsigned release variants.
- Backlog validation initially assumed every story remained in `todo` and did not support the documented lifecycle folders.

The VIP test now derives a guaranteed mismatching key without changing the configured secret. Release signing now inspects the resolved Gradle task graph for release artifact tasks, including aggregate invocations. Backlog validation now scans all three lifecycle folders and requires `Status` to match the folder.

## Round 2 — 9.5/10

| Dimension | Score | Evidence |
|---|---:|---|
| Correctness and acceptance criteria | 1.9/2.0 | All five lint errors are fixed; local signing inputs are externalized and release artifact tasks fail closed without them. |
| Unit-test quality and coverage | 1.5/1.5 | 137 Android unit tests and four signing contract tests pass, including all-locale resource ownership. |
| Widget/UI-test quality and coverage | 1.0/1.0 | Language icon tint tests pass; signing UI is reviewed as not applicable. |
| Integration-test quality and coverage | 1.5/1.5 | All 42 device tests pass, including manifest permission, locale resources and the repaired VIP negative case. |
| Approved-device smoke results | 1.0/1.0 | User-approved Pixel 7 Pro, Android 17/API 37, online via validated VPN; install and 658 ms cold launch pass with no fatal app log. |
| Security, privacy and Play readiness | 0.7/1.0 | Keystore/passwords are removed from HEAD and release tasks fail closed. Rotation/revocation and history cleanup require publisher access. |
| Performance, lifecycle and regression risk | 1.0/1.0 | No performance path changed; the complete unit/device suites and cold launch pass. |
| Maintainability and documentation truth | 0.9/1.0 | Signing setup, contract checks, lifecycle-aware backlog validation, warning triage and residual owner actions are recorded. |
| **Total** | **9.5/10** | **Qualifies for push under the user's score gate.** |

## Verification record

- `python3 scripts/test_release_signing_contract.py`: 4/4 pass.
- `python3 doc/task/validate_backlog.py`: 40 stories, zero cycles; 37 todo, one in progress and two done.
- `./gradlew testDevDebugUnitTest lintDevDebug assembleDevDebug`: pass; 137 unit tests, zero lint errors, 166 warnings, debug APK built.
- `./gradlew connectedDevDebugAndroidTest`: 42/42 pass on Pixel 7 Pro.
- Release artifact tasks without credentials: direct `assembleProductionRelease` and aggregate `:app:assemble --dry-run` both fail early with the documented signing error.
- `git diff --check`: pass.
- Proposed-diff secret scan: pass; no previous signing value, private-key block or credential value appears in added lines.
- `git diff -- store-assets`: empty.

## Independent review

An independent Codex audit confirmed the Android-only scope and found the aggregate Gradle-task signing gap recorded in Round 1. The implementation was corrected to inspect the resolved task graph. The reviewer then reached its usage limit before producing a final numeric score, so the Round 2 score uses deterministic local evidence and records the reviewer limitation.

## Residual owner actions

SEC-001 remains in `inprogress` because repository code cannot determine or change Play Console ownership:

- Identify whether the exposed key is an upload key or app-signing key.
- Rotate/reset it through the correct Play App Signing flow and replace CI secrets.
- Validate an upload on a non-production track and revoke old access where supported.
- Coordinate any Git-history rewrite and force-push with every clone owner.

Pushing the HEAD cleanup is still beneficial because fresh clones stop receiving the exposed artifact and plaintext credential. History must continue to be treated as compromised until the owner actions finish.
