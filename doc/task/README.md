# Fisheye Lens Launcher — Product & Engineering Backlog

> **This file is the live status source of truth** for backlog state and test/build
> evidence (each story below carries its own point-in-time counts as it lands).
> Other docs under `doc/` predate this workflow and are marked superseded — treat
> their numbers as historical, not current. For a fresh test count, run
> `./gradlew testDevDebugUnitTest`.

> Audit baseline: 2026-09-05 · Branch: `dev` · Scope: Android app + tests/resources/config/docs + `store-assets`

## Working agreement

- Workflow: move one story file from `todo` → `inprogress` → `done`; do not copy it.
- Filename: `{priority}-{domain}-{id}-{slug}.md`; `domain` is the owning subsystem such as `sec`, `core`, `store`, or `fish`.
- Priority: P0 blocks release, P1 is required for release candidate, P2 is the next improvement wave, P3 is maintenance.
- Estimate: Fibonacci story points `1, 2, 3, 5, 8, 13`; split anything above 8 before implementation.
- Evidence: `confirmed` is proven by current code/build, `conditional` depends on deployment/runtime, `decision` records an explicit owner choice, and `idea` is a product hypothesis.
- A story is done only after its acceptance criteria, tests, audit notes, and documentation are complete.
- Every implementation case must include the applicable unit, widget/UI, integration, and smoke coverage. Any omitted layer requires a written `Not applicable` rationale reviewed during audit.
- Every delivery wave ends with a fresh code-change audit scored on a 10-point rubric. Push is allowed only when the score is **strictly greater than 9.0**, all code-controlled in-scope checks pass, and approved-device smoke testing passes. External owner actions may remain `inprogress` only when the audit records them explicitly.

## Audit baseline

- 374 tracked files and approximately 63,791 lines were reviewed using the code knowledge graph, direct source inspection, builds, lint, and an independent Codex audit.
- `./gradlew testDevDebugUnitTest`: 136 tests passed, 0 failed.
- `./gradlew lintDevDebug`: failed with 5 errors and 167 warnings.
- `store-assets`: `bun run build` passed.
- Initial worktree was clean.
- AGY's first report referenced another repository and was rejected; its clean retry timed out. Claude could not run because its session limit was reached. Neither is treated as evidence.

## Product decisions

- ✅ Backlog strategy: balance release hardening with visible user value.
- ✅ Story structure: one Markdown file per independently deliverable story.
- ✅ Differentiation direction: prioritize **Fisheye Smart** features implemented locally on device.
- ✅ FEAT-001 search-first app navigation completed, with only its directly required CORE/A11Y integration seams included.
- ✅ FEAT-002 Smart organization completed with its CORE-001 and DB-001 foundations.
- ✅ Device policy from FEAT-001 through CORE-002: qualification and smoke used the designated TECNO KJ7 only; Pixel excluded.
- ✅ Device policy update (2026-09-06, owner decision): qualification and smoke now use the designated **Samsung S24 Ultra (SM_S928B, serial R5CX613VZBR)** only; no other physical device or emulator. Applies from CORE-002 onward. Prior TECNO KJ7 evidence in `done/` stays valid as historical record.
- ⚠️ One-off exception (2026-09-06, owner decision): S24 Ultra was not connected during SEC-003; owner explicitly approved TECNO BG6 for that round's build/run/smoke only. The S24-Ultra-only policy is unchanged for all other/future stories.
- ⏸️ Deferred (2026-09-06, owner decision): TEST-001 (CI trustworthy gates) and checking whether ADS-001 has unblocked VIP-001/REL-002 are both pushed to next month. TEST-002 and AUDIT-001 stay blocked as a consequence (they depend on TEST-001). The loop moves to unblocked P2 work in the meantime.
- ⚠️ Device policy update (2026-09-07, owner decision): owner authorized the assistant to self-select the physical smoke-test device each round for the remainder of the code-loop, instead of confirming per story. The S24-Ultra-preferred policy is otherwise unchanged; when S24 Ultra isn't connected, the assistant picks the best available connected device and records model/serial in the story's evidence, same as prior one-off exceptions. UI-001 used TECNO KJ7 under this standing authorization.
- ❌ TEST-001 (CI trustworthy gates) declined by owner (2026-09-07): owner does not want CI-gate process work in this loop. TEST-002 and AUDIT-001 remain blocked as a consequence (they depend on TEST-001); this is a standing decision, not a scheduling delay.
- ❌ STORE-001/002 confirmed out of scope (2026-09-07, owner decision): `store-assets` is internal tooling, not the shipped app, and stays excluded per the existing skip decision below — the loop only picks app-facing stories.
- ⚠️ Device policy update (2026-09-12, owner decision): the 2026-09-07 self-select authorization above is **revoked**. Owner reissued the hard ban — only the Samsung S24 Ultra (SM_S928B, serial `R5CX613VZBR`) may be used for build/run/install/smoke; no other physical device or emulator, even when S24 Ultra isn't connected. Triggered by an incident during LEAK-001 where `./gradlew connectedDevDebugAndroidTest` fanned out to a second attached device (Gradle's connected-test task cannot be scoped to one serial); corrected by re-running verification via direct `adb shell am instrument` against only S24 Ultra. Applies from LEAK-001 onward.
- ⚠️ Device exception (2026-09-13, owner decision): S24 Ultra `R5CX613VZBR` is damaged and unavailable. For `SEARCH-005` only, owner authorizes Pixel 7 Pro (`cheetah`, serial `2B051FDH3006MU`) as the designated build/run/install/smoke device; TECNO remains fallback only if Pixel is unavailable. This exception is recorded per story and does not silently change the general S24 policy.
- ❌ A11Y-001 declined by owner (2026-09-12): offered as the recommended next pick right after LEAK-001 shipped; owner declined ("A11Y-00 nên skip, tôi không thích") with no further rationale. `FEAT-003`, `FEAT-004`, and the `FISH-*` "Fisheye Smart" line all depend on A11Y-001 and stay blocked as a consequence — this is a standing decision, not a scheduling delay. `DISPLAY-001` picked instead for this round.
  - ✅ Superseded (2026-09-20): A11Y-001 (font scale/TalkBack/RTL) shipped anyway, followed same-day by `FEAT-003` (list mode) and `FEAT-004` (adaptive layouts) - all three now in `done/`. No record of the owner reversing the 2026-09-12 decline exists in this repo; noting the actual outcome here rather than leaving the blocked-consequence framing above stale. `FISH-003/004/005` are unblocked by this (their other individual dependencies still gate each one separately - see their own files).
- ❌ FISH-002 declined by owner (2026-09-20): offered as the recommended next pick after UI-002 and SEARCH-005; owner explicitly declined ("FISH-002 không cần làm đâu, skip đi và note lại lí do"). Edge alphabet scrubber is skipped and excluded from the implementation loop.
- ⚠️ Device exception (2026-09-25, owner decision): S24 Ultra not connected; owner authorized Pixel 7 Pro (`2B051FDH3006MU`) for this session (Keep Screen On follow-up and PERF-004 onward). TECNO KJ7 attached but untouched. S24-only policy otherwise unchanged.
- ❌ ADS-001-dependent chain declined by owner (2026-09-22): with backlog otherwise exhausted (everything else blocked on SEC-001's Play Console owner access or ADS-001's external Ad SDK team), owner explicitly declined the entire chain that depends on ADS-001 ever landing ("không đụng ADS-001, chính thức declined cả chuỗi phụ thuộc") rather than leaving it as an indefinite scheduling delay. Declined-as-consequence: `VIP-001`, `REL-002`, `MON-001`, `INSIGHT-001`, `FISH-001`, `FISH-003`, `FISH-005` - each file keeps `Status: todo` and gets its own dated "Owner decision" section (same convention as `FISH-002` above), staying in `todo/` as historical record. ADS-001 itself is untouched (still `inprogress — external`); this decline does not ask the assistant to implement it. Revisit any of these only once ADS-001 actually ships or the owner explicitly reopens a specific story.

## ✅ Implemented

- REL-001 — Android lint release blockers cleared; unit, widget/UI, integration and Pixel smoke evidence recorded.
- SEC-002 — Closed by the owner's decision to exclude `store-assets`; all Wave 0 code changes in that directory were reverted.
- FEAT-001 — Search-first app navigation; local deterministic ranking, recent history, accessibility, locale/offline behavior and TECNO KJ7 performance smoke completed.
- CORE-001 — Installed-app refresh is application-owned, debounced, cancel-latest and generation guarded.
- DB-001 — Room access is asynchronous and atomic with unique component identity, exported schemas and explicit migrations.
- FEAT-002 — Local favorites, folders, pinned zones, drag/menu ordering and reinstall recovery completed on TECNO KJ7.
- CORE-002 — Icon cache identity/invalidation fixed (composite key: component + package version + icon-pack identity), self-audited 9.15/10, S24 Ultra smoke and 172 unit + 5 instrumented tests all pass (2026-09-06).
- PERF-002 — Bounded icon-cache memory: `BitmapCache` budget now derived from `ActivityManager.memoryClass` capped at a measured-icon-size ceiling, instead of `Runtime.maxMemory()` (which scaled with `largeHeap`). `onTrimMemory` is now tiered (partial trim at `RUNNING_LOW`/`RUNNING_CRITICAL`/`UI_HIDDEN`, full clear only at `BACKGROUND`+/`onLowMemory` — previously any level `>= RUNNING_CRITICAL`, including the very common `UI_HIDDEN`, cleared everything). Evicted icons now reload asynchronously via a new on-demand path instead of staying blank. `android:largeHeap` removed after real-device profiling showed no OOM. Found and fixed a live NPE in `UtilBitmap.packageNameToBitmap` during testing. 9 unit + 1 widget + 6 integration tests added; full regression 218/218 unit, 97/97 connected (all pre-existing flaky failures also passed this round). Self-audited **9.85/10** (2026-09-06, TECNO KJ7).
- PREF-001 — Stable preference IDs: `SortType`/new `BackgroundMode` enum now persist by `.name` (not ordinal/free-text), with read-time write-through migration from legacy values and safe defaults on corrupt/unknown data — every enum value individually verified, not sampled. Also fixed a live bug found during investigation: the icon-pack "default" sentinel was a hardcoded English literal compared against a translated dialog string, desyncing on non-English locales — now a stable, never-displayed marker. 19 unit + 5 widget + 5 integration tests pass; full regression 209/209 unit, 89/90 connected (1 pre-existing unrelated failure). Real-device smoke on two devices in two live app languages (Samsung SM_A115F/Vietnamese, TECNO KJ7/Thai) confirmed the fix live. Self-audited **9.85/10** (2026-09-06).
- LAUNCH-001 — Fixed `res/xml/shortcuts.xml`'s `targetPackage` (was `com.mckimquyen`, the code namespace; corrected to `com.mckimquyen.lenslauncher`, the real applicationId). A resource-indirection approach (`@string/...` sourced from Gradle's `applicationId`) was tried and found broken on real hardware — the OS shortcut parser does not resolve `@string` references there — so the fix is a corrected literal plus a regression test. 6 new integration tests (real `ShortcutManager.manifestShortcuts` registry) pass; full connected suite 78/80 (2 pre-existing, unrelated, device-capability failures on the weaker smoke device); 190/190 unit tests unchanged. Self-audited **9.55/10** (2026-09-06, Samsung SM_A115F, owner-approved one-off exception to the S24 Ultra policy).
- PERF-001 — LensView render hot-path: extracted `LensGridCache` to cache grid geometry and each cell's base `RectF`, previously recomputed from scratch every `onDraw` (continuous while dragging); hot loop reuses a single scratch `RectF` for the fisheye shift/scale math instead of allocating one per cell per frame. 230/230 unit, 98/98 connected pass; real-device (`dumpsys gfxinfo`) frame-timing on TECNO KJ7 shows no regression against the documented 60/90/120 Hz budgets, though the win wasn't measurable on this specific fast device/app-count combination (disclosed honestly — the allocation/recompute reduction is proven deterministically by unit tests instead). Self-audited **9.30/10** (2026-09-07, TECNO KJ7).
- UI-001 — Material You theme migration + search bar revamp: base theme migrated to `Theme.Material3.DayNight(.NoActionBar)`, real Android 12+ dynamic color enabled app-wide (`DynamicColors.applyToActivitiesIfAvailable`), status/navigation bars follow dynamic color (fixed a hardcoded `@color/colorPrimary` background masking it). Home-screen search rebuilt on real `com.google.android.material.search.SearchBar`/`SearchView` (morph-into-panel animation), existing search/rank/launch/history logic preserved untouched. New `KEY_SHOW_SEARCH_BAR` settings toggle (default on) added end-to-end. 222/222 unit, 98/98 connected tests pass; real-device smoke on TECNO KJ7 confirmed the animation and both toggle directions, including catching and correcting an initial mis-tap from a coordinate-scaling error. Self-audited **9.45/10** (2026-09-07, TECNO KJ7, owner-authorized self-selected device for this session).
- SEC-003 — WebView and exported-component hardening: `SuperWebViewActivity`/`ActAbout`/`ActVipManagement`/`SplashAct` set `exported=false` (no legitimate external callers); exact HTTPS host allowlist (`isAllowedWebViewUrl`) gates both the initial load and in-page navigation, replacing a bypassable substring check; dangerous schemes (`javascript:`, `file:`, `content:`, `data:`, `intent:`) rejected; WebView file/content access and mixed content disabled, Safe Browsing enabled; WebView removed from its parent before `destroy()`. Also fixed an unrelated same-day `app/build.gradle` typo that broke every debug build. 18 unit + 14 instrumented (5 hardening + 4 security-integration + 5 widget) tests pass, plus 3 pre-existing tests confirmed non-regressed (17/17 connected suite); real-device proof that an external `am start` is denied (`START_CLASS_NOT_FOUND`); lint 0 errors. Self-audited **9.65/10** (2026-09-06, TECNO BG6, owner-approved one-off exception to the S24 Ultra policy — see the story file for the full rubric).
- LEAK-001 — `FrmApps.onDestroyView()` now detaches its RecyclerView adapter and nulls `rvApps`/`progressBarApps`/`utilSettings`/`appAdapter`, matching the pattern `FrmLens` already used (BUG-14); prior memory-leak documentation had wrongly claimed this was already complete. 5 new widget tests (`FrmAppsWidgetTest`, reflectively proving every field is cleared post-destroy) plus 2 new integration tests (`FrmAppsLifecycleIntegrationTest`, reproducing the real `ViewPager2`/`FragmentStateAdapter` tab-switch trigger and an `Activity.recreate()` rotation, both proving apps reload correctly from `RAppsSingleton`); full 105-test instrumentation regression run (1 pre-existing unrelated flaky test isolated and confirmed passing), full unit regression unaffected, lint 0 errors. Self-audited **9.9/10** (2026-09-12, Samsung S24 Ultra only, verified via direct `adb shell am instrument` after a Gradle connected-task device-scoping incident — see the device policy update above).
- DISPLAY-001 — `BaseActivity` no longer forces the highest display refresh rate on every screen: new `wantsHighRefreshRate()` defaults to `false` (system chooses) and only `ActHome` (the live fisheye grid) overrides it to `true`; `onPause()` now releases the requested mode (`preferredDisplayModeId = 0`) so it isn't held while backgrounded; a new pure `shouldRequestHighRefreshRate(batterySaverOn, thermalStatus)` policy function backs off under battery saver or `THERMAL_STATUS_MODERATE`+ throttling. 7 new unit tests cover every policy branch; 4 new widget/integration tests (`BaseActivityRefreshRateWidgetTest`) prove the real `Window`/`Display` side effect on `ActHome` vs `ActSettings` across resume/pause. Full 109-test instrumentation regression pass (0 failures — the prior round's flaky search test did not reproduce), full unit regression pass, lint 0 errors. Self-audited **9.8/10** (2026-09-12, Samsung S24 Ultra only).
- LINT-001 — Owner-picked filler once the real backlog ran dry (everything else chains to declined `A11Y-001`/`TEST-001` or externally-blocked `ADS-001`/`VIP-001`/`SEC-001`). Cleared the safe, mechanical, zero-dependency subset of pre-existing lint warnings (13 of 171, across `ApplySharedPref`/`DefaultLocale`/`SwitchIntDef`/`ObsoleteSdkInt`/`UselessParent`) while explicitly leaving `ContentDescription` (accessibility, `A11Y-001` territory) and the larger `UnusedResources`/`UseKtx`/`HardcodedText` buckets untouched for a future, separately-scoped round. Added `UtilNightModeUtilTest` (5 tests) and `ActAboutWidgetTest` (3 tests) closing two zero-prior-coverage gaps found along the way. Lint 171 → 157 warnings, 0 errors throughout; full 112-test instrumentation regression and full unit regression pass. Self-audited **9.8/10** (2026-09-12, Samsung S24 Ultra only).
- LINT-002 — Second lint cleanup round, same owner-picked-filler situation. Fixed `Typos`/`ButtonCase` (`"Ok"` → `"OK"`, base locale only — other locales' deliberate translations left untouched), `SmallSp`, `RtlSymmetry`, `Autofill` (`act_vip_management.xml`); recognized `AppBundleLocaleChanges` (`LocaleHelper`) and `MergeRootFrame` (`act_home.xml`) as lint false positives and suppressed both with a documented reason instead of forcing an incorrect change. Deliberately excluded `PluralsCandidate`/`NotifyDataSetChanged`/`TooDeepLayout` from this "small" round — each needs its own properly-scoped story (multi-locale `<plurals>` work, a `DiffUtil` adapter redesign, and a structural layout flattening, respectively). Lint 157 → 150 warnings, 0 errors; full 112-test instrumentation regression (1 pre-existing unrelated flake, same as prior rounds) and full unit regression pass. Self-audited **9.85/10** (2026-09-12, Samsung S24 Ultra only).
- PERF-003 — Replaced `notifyDataSetChanged()` with `DiffUtil`-driven granular updates in `AppAdapter` (Settings app list) and `SearchResultAdapter` (search results), via a new shared `AppDiffCallback` (identity by `packageName`+`name`, matching the codebase's existing app-identity convention). Closes the `NotifyDataSetChanged` deferral from `LINT-002`. 9 new unit tests (`AppDiffCallbackTest`) cover every diff outcome and identity/content edge case; `AppAdapterWidgetTest` (new — `AppAdapter` had zero prior coverage) proves correct visible order/content across insert/remove/reorder/content-change/empty; a new `FrmAppsLifecycleIntegrationTest` case exercises the real in-place production update path (`AppEventManager.notifyAppsLoaded()` while the Apps tab stays alive) that the file's existing tests structurally couldn't reach. Diagnosed and disclosed a newly-surfaced cross-test background-app-scan flake (pre-existing, unrelated to this diff). Lint 150 → 148 warnings, 0 errors; full 115-test instrumentation regression (1 pre-existing unrelated flake) and full unit regression pass. Self-audited **9.85/10** (2026-09-12, Samsung S24 Ultra only).
- LINT-003 — Removed the entire `UnusedResources` bucket (58 warnings): 22 unused drawable/PNG files (including an orphan `drawable-hdpi/ic_launcher.png` duplicating the real `mipmap` launcher icon), 10 unused dimens (plus 2 dead qualifier-specific overrides), 28 unused string keys across `strings.xml`/`strings_vip.xml` and all 17 locale files (476 individual lines), and one unused style (`RoundedDialogTheme`). Every resource was independently verified unreferenced (grep across all source/XML, manifest check, and confirming the app's only dynamic resource lookup always targets a third-party icon pack, never its own resources) before deletion, guarding against the `LAUNCH-001`-style false-positive risk. Lint 148 → 87 warnings, 0 errors; disclosed one unrelated side-effect lint hint (`IconMissingDensityFolder`, pre-existing icon-density gap, out of scope). Full 115-test instrumentation regression: **0 failures** (first fully green full-suite run this loop — the recurring `AppSearchIntegrationTest` flake did not reproduce). Self-audited **9.9/10** (2026-09-12, Samsung S24 Ultra only).
- BUILD-002 — Bumped the Gradle wrapper 8.14.3 → 8.14.5 (`AndroidGradlePluginVersion`, 1→0) via `./gradlew wrapper --gradle-version 8.14.5` — a patch-level Gradle bump, not an AGP upgrade (initially mischaracterized as the latter; corrected after reading the warning text, which only flags `gradle-wrapper.properties`). This was **the last code-fixable lint category**: lint is now 20 warnings, all `ContentDescription` (declined `A11Y-001` territory) or icon-asset design work, not code. Side finding disclosed: the regeneration dropped a pre-existing `android.enable16kPageAlignment=true` line from the wrapper properties file — investigated and confirmed it was already dead (wrapper properties aren't merged into the build, and the property isn't a real AGP 8.13.0 flag either), not something this change disabled. Full 119-test instrumentation regression: **0 failures**, the first run this session where the previously-flaky search test and the real-network WebView tests both passed together (device Wi-Fi had dropped mid-session and was reconnected before this run). Self-audited **10.0/10** (2026-09-12, TECNO KJ7 — fourth re-authorized one-off, S24 Ultra still disconnected).
- LINT-008 — `TooDeepLayout` (1→0): traced `act_vip_management.xml`'s deepest chain (10+ levels) to its 3 repeated "icon + two-line text" feature rows, each 2 nested `LinearLayout`s; replaced each with a single `ConstraintLayout` (already transitively on the classpath, used elsewhere e.g. `a_splash.xml`), removing 1 level per row and bringing the file under threshold. Deliberately left the top-of-tree `FrameLayout`/`LinearLayout` manual-stacking pattern untouched (a `CoordinatorLayout` rewrite would remove another level but risks real scroll/toolbar behavior changes, out of scope here). Verified with a temporary in-process screenshot (the activity is `exported=false`) that the 3 feature rows render pixel-identical to before. Lint 22 → 21 warnings, 0 errors. Full-suite regression ran clean through 27/29 classes before the TECNO KJ7 lost network connectivity and hung the remaining real-HTTPS `SuperWebViewActivity*` tests (unrelated to this diff — killed and disclosed rather than forced); targeted `FVipManagementWidgetTest` (8/8) + `VipPluralsInstrumentedTest` (4/4) pass. Self-audited **9.85/10** (2026-09-12, TECNO KJ7 — third re-authorized one-off this session, S24 Ultra still disconnected).
- LINT-007 — `PluralsCandidate` (2→0, plus a follow-up `MissingQuantity`, 8→0): `vip_entry_redeemed`/`vip_activation_success_message` converted from a hardcoded `%d days` string to `<plurals>`, with correct CLDR categories per language (no-distinction for vi/zh/ja/ko/th/km/lo/in; one/other for en/de/es/fr/it/pt/hi; one/few/many/other for ru; the full zero/one/two/few/many/other for ar). Lint's `MissingQuantity` follow-up caught that es/fr/it/pt also need a `many` category — fixed properly rather than dismissed. A Robolectric unit-test attempt failed for an environmental reason (its `getQuantityString` shadow couldn't resolve quantity items on this SDK level despite the resources being provably correct) — moved to a real-device instrumentation test instead, which also caught and fixed a locale-sensitivity bug in the test itself (device was in Vietnamese, correctly returning Vietnamese text). Lint 30 → 22 warnings, 0 errors. Full 119-test instrumentation regression: **0 failures** (fully green, including the previously-flaky `AppSearchIntegrationTest`). Self-audited **9.75/10** (2026-09-12, TECNO KJ7 — S24 Ultra still disconnected, re-authorized one-off).
- LINT-004/005/006 — Three more lint categories cleared in the same round, on owner's explicit request. **LINT-004 (`UseKtx`, 40→0)**: replaced `Uri.parse`/`Color.parseColor`/`SharedPreferences.edit()...apply()`/`Bitmap.createScaledBitmap`/`Bitmap.createBitmap` with their `androidx.core` KTX equivalents across `ActVipManagement.kt`, `ext/Activity.kt`, `ext/Context.kt`, `util/BitmapCache.kt`, `util/LocaleHelper.kt`, `search/SearchHistoryStore.kt`, `feature/vip/VipPrefs.kt`, `ext/View.kt`, `util/WebViewSecurity.kt`; added `ActivityKtUriTest` (10 tests, closing a zero-prior-coverage gap) and disclosed 3 genuinely dead functions found along the way (`launchSystemSetting`, `sendEmail`, `Drawable.toBitmap()` — out of scope to remove here). **LINT-005 (`HardcodedText`, 16→0)**: extracted UI strings across 5 layouts into `strings.xml`, translated into all 16 non-English locales (this project treats `MissingTranslation` as a build-blocking error, caught on the first English-only pass); two placeholder `TextView`s traced through code and confirmed always-overwritten before display, converted to `tools:text` instead of wasting a resource; brand name and a directional glyph marked `translatable="false"` matching existing project convention. **LINT-006 (`Overdraw`, 7→0)**: 1 genuine fix (`a_splash.xml`, verified colorimetrically identical to the theme's own default background in both light/dark by decompiling the Material3 AAR), 4 documented lint false positives suppressed (`tools:ignore="Overdraw"`: two ViewPager2 Fragment pages, two ripple-only `selectableItemBackground` rows), and 2 real-but-out-of-scope cases left deliberately unfixed with a documented reason (`act_settings.xml`/`act_vip_management.xml` share a theme with two other activities that have no explicit background override — moving the color into the theme risks a cross-activity visual regression that wasn't verified). Lint 87 → 24 warnings total across the three, 0 errors throughout. **Device incident**: S24 Ultra dropped off `adb devices` mid-round; owner explicitly authorized a one-off TECNO KJ7 fallback (recorded in memory) rather than the assistant self-deciding. Full 115-test instrumentation regression on TECNO KJ7: 1 pre-existing unrelated flake (`AppSearchIntegrationTest`, same as prior rounds), full unit regression pass. Self-audited **9.9/9.85/9.75 out of 10** respectively (2026-09-12).
- UI-002 (partial) — Home search overlay revamp: search panel now sits in a `MaterialCardView` over a real 50%-opacity scrim (`app:backgroundTint` on the Material `SearchView`, the only supported way to theme its panel — its background field is private with no runtime setter), result rows share one `DividerItemDecoration` instead of per-row cards (icon 44dp→48dp). Icon-size default now auto-scales to device (`UtilSettings.calculateAutoDefaultIconSize`, keyed off `smallestScreenWidthDp`), is rounded to the integer dp used by the seekbar, and is materialized on first settings initialization without overwriting user choices. `UI-003` (settings shell restyle) and `UI-004` (VIP/About/Splash full redesign) split out and remain in `todo`.
- UI-002 follow-up (2026-09-13) — Fixed the dynamic icon-size default mismatch: first `UtilSettings` initialization now materializes the rounded device-specific value in `SharedPreferences`, so the renderer, settings screen and seekbar use the same dp. Added unit coverage for persistence, Android SharedPreferences integration coverage for first-run/explicit/clamped values, and device widget coverage on Pixel 7 Pro/Samsung SM-S928B; explicit user-selected sizes remain untouched.
- UI-003 — Settings shell Material3 restyle: `TabLayout` given `Widget.Material3.TabLayout.Secondary` (pill/underline indicator, dynamic color), `FrmApps`'s app-row `CardView` and `FrmSettings`'s 3 card sections swapped to `MaterialCardView` (same ids/attrs, zero code change needed — `AppAdapter`'s `CardView` field just accepts the subtype). Row height/icon sizing already matched M3 spec by construction (48dp icon + margins ≈ 72dp). `FrmAppsWidgetTest`/`AppAdapterWidgetTest`/`FrmAppsLifecycleIntegrationTest` 10/10 pass, lint unchanged (0 errors/20 warnings). Self-audited **9.5/10** (2026-09-12, TECNO KJ7).
- UI-004 — VIP/About/Splash: investigation found `ActVipManagement`/`ActAbout` already have substantial motion (slide-in, pulse/shimmer, confetti, count-up; staggered cards, circular reveal) not visible from layout alone — scoped down from a ground-up rewrite (owner briefed and chose to proceed with only the genuinely valuable/safe changes): `SplashAct` given a fade+scale entrance animation (disclosed as currently dead code — its intent-filter is commented out, kept as-is per owner decision), and 2 truly-generic hardcoded VIP colors (revoke button, disabled button text) switched to `?attr/colorError`/`?attr/colorOnSurfaceVariant` — VIP-gold branding colors left untouched (contrast-driven, not a bug, same conclusion as `UI-005`). `SEC-003`/`LINT-008` hardening unaffected. Self-audited **9.0/10** (2026-09-12, TECNO KJ7).
- SEARCH-001 — Fuzzy typo-tolerant app search: `AppSearchEngine` gains a pure `levenshteinDistance` fallback (worst-tier, below every exact/substring match), gated by a minimum token length (3) and a length-scaled edit threshold (1 for ≤5 chars, 2 for longer) to avoid noisy matches. 7 new unit tests; existing accent/case-insensitive and perf-budget tests unaffected. Live-verified on TECNO KJ7: "faceboook" (typo) still surfaces Facebook. Self-audited **9.8/10** (2026-09-12).
- UI-008 — Whole-app theme-attr-override audit (owner request, 2026-09-13): reviewed every style declaration for the same shadowed-dynamic-color bug class UI-005 found. `AppTheme`/`HomeTheme` clean (only the already-fixed roles). Found and fixed one remaining instance: `LightAlertDialogCustom` (the "Điều khoản và Chính sách bảo mật" consent dialog — corrected an earlier wrong claim in this session that misidentified it as Google's UMP; it's this app's own `showDialog2`) had parent `Theme.AppCompat.Light.Dialog.Alert` (a full theme, not overlay), whose own ancestry shadowed the dialog's inherited dynamic colorPrimary — title rendered nearly invisible. Switched to `ThemeOverlay.AppCompat.Dialog.Alert`. Confirmed live on Pixel 7 Pro. Self-audited **9.6/10**.
- SEARCH-003 — App shortcuts (via `LauncherApps`, safe SecurityException fallback for non-default-launcher installs) + inline row actions (info/pin start/pin end/unpin/uninstall) on search results. Extracted shared `UtilApp.appInfoIntent`/`uninstallIntent` used by both `AppAdapter` and the new `SearchResultAdapter` menu — no duplicated logic. 5 new tests. Smoke-verified menu + pin action on Pixel 7 Pro (shortcuts themselves unverifiable live — neither test device is the default launcher). Self-audited **9.2/10** (2026-09-13).
- SEARCH-002 — Zero-permission local quick actions: calculator, offline unit conversion (length/weight/temperature), timer quick-set, battery %, and Settings deep-links surfaced above normal app search results (`search/QuickActionEngine.kt`, tried in that priority order, first match wins). 25 new unit tests + 4 new widget tests. Smoke-verified end-to-end on Pixel 7 Pro (one-off exception — TECNO KJ7 in active personal use this round): "12*7" → "84", "wifi" opens the real Wi-Fi Settings screen. Self-audited **9.5/10** (2026-09-13).
- UI-016 — Expanded SETTINGS_KEYWORDS from 16 to ~45 entries (brightness/storage/battery saver/notifications/accessibility/airplane mode/NFC/VPN/app manager/developer options/sync/print/keyboard/device info/system update) after clarifying with the owner (`AskUserQuestion`) that a "true Spotlight-style index all Settings preferences" feature is not achievable for a third-party app (no public API; every launcher including Nova/Lawnchair ships a hardcoded table like this one). Also found and fixed a real legibility bug while investigating: `tvQuickActionLabel` had no explicit textColor/size/weight, rendering technically-correct but too faint/small to notice against the scrim (confirmed via a zoomed screenshot crop, not a logic bug - added logging then removed it once root-caused) - now bold 16sp `colorOnSurface`, plus a trailing "›" chevron on Settings/timer-type rows that previously looked blank on the right. 142/143 pass on Pixel 7 Pro (1 pre-existing disclosed flake). Self-audited **9.2/10**.
- UI-015 — Fixed 2 bugs owner found live right after UI-014: (1) grid icons under the search pill became unclickable - root cause: `setupEdgeToEdge2`'s `setPadding()` applied directly to `lensViews` did nothing, since `LensView` is a leaf custom View that never reads its own padding in its fisheye geometry/touch-hit math (confirmed by grep) - fixed with the same margin-based helper already used for `searchBar`, since a margin genuinely shrinks a `match_parent` child's laid-out bounds regardless. (2) search results list "not full height" - the hand-computed 384dp/6-row cap (a UI-002-era leftover) left most of the screen empty once UI-011's all-apps fallback could show dozens of apps; replaced with a `layout_weight="1"` fill-remaining-space pattern, deleting the manual height-calculation code entirely. 141/143 pass on Pixel 7 Pro (2 pre-existing disclosed flakes; a 3rd apparent failure was confirmed to be the owner's own live SEARCH-004 permission grant on this device, not a regression). Self-audited **9.4/10**.
- UI-014 — Status bar harmony via real edge-to-edge content, not just Window paint (owner: "kết hợp cả 2", 2026-09-13): `rootLayout` no longer inset-padded at all; `lensViews` and `searchBar` get their own targeted padding/margin instead, so `searchView`'s scrim now physically reaches the true top edge (mirrors UI-013's bottom fix) - `Window.setStatusBarColor`/`setNavigationBarColor` kept as a fallback per the owner's chosen option. 141/143 pass on Pixel 7 Pro (2 pre-existing disclosed flakes, unrelated). Self-audited **9.5/10**.
- FISH-004 — Adaptive lens physics and haptic profiles (2026-09-22, unblocked once PERF-001/DISPLAY-001/A11Y-001 all landed): 3 named presets (Gentle/Standard/Snappy) as quick-apply buttons over the existing distortion/scale/animation-time sliders in `FrmLens` (Standard == today's exact defaults, so nothing changes for users who don't touch it) - no new parallel state, no change to `LensView`'s rendering/hit-test math. New `LensPhysicsPolicy.shouldReduceLensMotion()` (mirrors `DISPLAY-001`'s `shouldRequestHighRefreshRate` pattern) suppresses show/hide animation and haptics under reduced motion, battery saver, or moderate+ thermal throttling. Found and fixed a real bug in this story's own first pass: preset button text wrapped mid-word ("Stand ard") on a real device, not caught by lint or persisted-value assertions - fixed and re-verified live. 8 new unit tests, 3 new widget tests, 1 new real-device Settings.Global integration test (421/421 unit, all new androidTest pass). Live-verified on Samsung SM-S928B (S24 Ultra) and TECNO BG6: full 203-test connected regression, 201 pass (2 pre-existing unrelated, traced to commit `1638f87` outside this session). Self-audited **9.3/10**.
- UI-019 — Fix top-margin compounding on rotation + LensView double-reserved insets (owner-reported live on Samsung S24 Ultra, 2026-09-22): `ActHome.applyHomeColumnInsets()` read `searchBar`'s live (already-inflated) top margin as its own base on every call, growing the top gap on every `onConfigurationChanged`; fixed by reading a fixed `@dimen/home_search_top_margin`. `LensView.kt` also carried a pre-UI-013 leftover `OnApplyWindowInsetsListener` that double-reserved system-bar space on top of `ActHome`'s own margins and fired `invalidate()` on every insets dispatch - deleted. Confirmed live on S24 Ultra: `uiautomator` bounds stable across 2 rotations; grid now fills to the gesture-nav edge instead of stopping ~96dp short. `ActHomeMarginStabilityWidgetTest` + `LensViewInsetsRegressionWidgetTest` added (2/2 pass). Self-audited **9.3/10**.
- UI-020 — Fix continuous icon flicker (BitmapCache thrash) + wire up count headers/tablet clamp (owner-reported live on Samsung S24 Ultra, 2026-09-22): root-caused the flicker that survived UI-019 to `BitmapCache`'s fixed 300-icon/192px ceiling (~42MB) being smaller than this device's real 346-app inventory on an unpaged, show-everything-at-once grid - the overflow icons were stuck in a permanent evict/reload/invalidate loop. Fixed via `TARGET_ICON_SIZE` 192→128px + `MAX_CACHED_ICONS` 300→1000 (real safety cap stays the memory-class-derived heap budget). Verified with an 8-screenshot 0.4s-interval pixel diff: 0% change post-fix vs. constant nonzero pre-fix. Bundled: wired the previously-orphaned `apps_count`/`search_results_count` plurals into `allAppsHeader`/`resultsSectionHeader` (a real, if minor, A11Y-001 gap - TalkBack never announced result counts), wired the previously-orphaned `home_content_max_width` FEAT-004 dimen into a symmetric-margin clamp via a new pure `UtilCalculator.calculateContentMaxWidthMargin` (unit tested; not live-verified on an actual tablet - none available), and deleted 16 dead resources found via a full lint audit (35→9 warnings). Found and fixed 2 of my own new test's race/isolation bugs rather than shrugging them off as flaky - one raced `SearchView.show()`, the other inherited an empty global app-list singleton left behind by `a11y.AccessibilityActionsIntegrationTest`'s incomplete cleanup (reproduced deliberately by forcing that adversarial run order, then fixed and re-confirmed). Full regression: 199 connected tests, 197 pass (2 pre-existing failures traced via `git log` to commit `1638f87`, outside this session, unrelated). 413/413 unit tests, lint 0 errors/9 warnings. Self-audited **9.3/10**.
- UI-017 — Home collapsed app drawer column fix (owner-reported edge-to-edge/search overlap, 2026-09-13): `act_home` is now hosted by one root `CoordinatorLayout` instead of a full-screen `searchCoordinator` overlay, with `LensView` laid out below a compact 48dp search rail (32dp horizontal margins) via runtime system-bar-aware margins. This keeps icons visible/touchable below the search affordance and preserves full-screen `SearchView` only while search is open. Added `ActHomeLayoutWidgetTest` to assert `LensView.top >= SearchBar.bottom` and compact search height; Samsung SM-S928B smoke screenshot confirmed `ActHome` focused with the revised top rail. Self-audited **9.3/10**.
- UI-018 — Search drawer flicker/icon/scroll fix (owner-reported, 2026-09-14): made the home inset margin update idempotent to stop relayout flicker, kept `LensView` invisible while `SearchView` is showing so the grid cannot draw/ghost behind search, and changed the search surface from 0.96 alpha to fully opaque. `SearchResultAdapter` now always shows a launcher placeholder on icon cache miss, reloads evicted icons asynchronously, dedupes in-flight icon loads, and moves `LauncherApps` shortcut lookup off the bind path with a per-package cache to reduce scroll jank. Added widget coverage for hidden grid/opaque scrim/cache-miss placeholder. TECNO KJ7 proof: targeted device tests 5/5 pass; smoke scroll on real app list showed icons present and `gfxinfo` jank 3.07% (p95 26ms, p99 125ms single tail spike). Self-audited **9.3/10**.
- SEARCH-004 — Permission-gated flashlight toggle + wifi SSID quick actions (owner-picked epic, shipped 2026-09-13): `CameraManager.setTorchMode`/`WifiManager` behind `CAMERA`/`ACCESS_FINE_LOCATION`+`ACCESS_COARSE_LOCATION` runtime permissions, "ask once, remember denial" pattern so a decline never nags again. Discovered mid-implementation that SSID reading needs location permission too (not just `ACCESS_WIFI_STATE` as originally scoped) - flagged via `AskUserQuestion`, owner chose to add it. Live-verified on Pixel 7 Pro: real CAMERA dialog → torch physically toggled; real location dialog (Precise/Approximate choice) appeared correctly for the SSID request. Play Console Data Safety form update still owner-pending before production release. Self-audited **9.0/10**.
- SEARCH-006 — Web search fallback (owner-picked epic, shipped 2026-09-13): a typed query with zero local matches (apps/shortcuts/quick actions all empty) now shows "Search the web for '<query>'", firing `Intent.ACTION_WEB_SEARCH` - respects the user's own default browser/search app, no hardcoded engine/URL, no network call from this app itself (confirmed live: Pixel 7 Pro's real chooser sheet offered Chrome/Google). `CLAUDE.md`'s Search section updated to note this one exception to "no network dependency". 2 new widget tests, 18/18 `AppSearchWidgetTest` pass. Self-audited **9.5/10**.
- UI-013 — Navigation bar harmony gap fix (owner: fix before Phase 3, 2026-09-13): root-caused to `ActHome`'s edge-to-edge inset padding clipping `searchCoordinator`/`searchView` short of the true bottom edge - harmless under 3-button nav (a real paintable bar surface still picked up the harmonized color) but visible under gesture nav (no such surface exists; only real content extending there can look harmonized). One-line fix (`paddingBottom=false`). Also fixed 2 pre-existing stale test expectations surfaced by a full-suite run on Pixel 7 Pro (not just the search test class) - one asserted UI-011's now-superseded pre-fallback behavior, one raced the async app scan after a fresh install. 138/139 pass (1 pre-existing disclosed font-scale flake, unrelated). Self-audited **9.4/10** (2026-09-13).
- UI-012 — Search bar customization settings (owner: "kết hợp cả 3", 2026-09-13): shipped the 2 lowest-risk pieces - per-quick-action toggles (calculator/unit/timer/battery/settings, `UtilSettings` + `QuickActionEngine.resolve()` gating + `llEmptyQuickActions` tile visibility) and custom search hint text; flagged search-bar position and a custom color/shape theme as higher-risk/lower-value and deferred (position fights `SearchBar`'s morph assumptions; a custom theme cuts against the Material You premise). Found (not fixed) verifying on Pixel 7 Pro/API 37: UI-009's status/nav bar harmonization relies on the now-deprecated `Window.setStatusBarColor`/`setNavigationBarColor` (no-ops under enforced edge-to-edge) - status bar still visually harmonizes for free via edge-to-edge, but the navigation bar does not (wallpaper shows through at the bottom edge) - a real gap flagged for a follow-up story. 16/16 `AppSearchWidgetTest` pass on Pixel 7 Pro. Self-audited **9.0/10** (2026-09-13).
- UI-011 — Empty-state revamp + flat results list (owner pick via `AskUserQuestion`, 2026-09-13): blank query now falls back to the full alphabetical app list plus a permanent 5-tile quick-actions row (calculator/unit convert/timer/battery/wifi, each prefills a working example into the search box reusing SEARCH-002's existing contextual row) instead of a bare empty state; the search-result list's `MaterialCardView` (owner: "frame inside a frame") removed entirely, content sits flat on the scrim per Material3's own full-screen-search spec. 16/16 `AppSearchWidgetTest` pass on TECNO KJ7. Self-audited **9.2/10** (2026-09-13).
- UI-010 — Three bugs found in owner's post-UI-009 review, fixed directly (no design trade-off): back button didn't close the search overlay (`OnBackPressedCallback` was an unconditional no-op, relied on `SearchView`'s own internal back handling which didn't fire in practice - fixed with an explicit `isShowing()` check), tapping a search result flashed the list to empty before the app launched (`hideSearch()`'s `clearText()` ran before `launchComponent` - reordered), and 16 user-facing strings (search-result pin/unpin menu, folder/favorite menu, "Show Search Bar" toggle) were untranslated in all 15 non-English/Vietnamese locales - translated all of them, removed the now-unneeded `tools:ignore="MissingTranslation"`. 14/14 `AppSearchWidgetTest` pass on TECNO KJ7. Self-audited **9.5/10** (2026-09-13).
- UI-009 — Search overlay revamp (owner request, 2026-09-13, after reporting UI-007's blur caused morph-animation lag): blur replaced with a single unified tonal (`?attr/colorSurfaceContainerHigh`) scrim (picked over deferred-blur/snapshot-blur/gradient-scrim via `AskUserQuestion`), status/navigation bar now painted the same tonal color while search is open (`ActHome.setSearchSystemBarsHarmonized`, luminance-based icon contrast), and the three search states (empty/recent/results) now have distinct section headers. Found and fixed two new bugs surfaced by this story itself: a `TransitionManager` crossfade left stuck `GhostView` overlays (reverted to instant visibility swaps), and `onResume()` was clobbering the harmonized bar color across a pause/resume while search stayed open (caught live on TECNO KJ7, fixed + regression-tested). 14/14 `AppSearchWidgetTest` pass on TECNO KJ7. Self-audited **9.3/10** (2026-09-13).
- UI-007 — Search overlay real frosted-glass blur (API 31+ `RenderEffect` on the lens grid, toggled with the search panel's show/hide transitions) replacing a flat-scrim-only look the owner found visually busy; two-tier design keeps a heavier flat scrim as the pre-31 fallback where blur isn't available. Caught and fixed a real state race (blur cleared on the wrong transition state) via a new widget test. Self-audited **9.5/10** (2026-09-13, Pixel 7 Pro).
- UI-006 — App-row lock control switched from a wide text button ("Lock"/"Unlock") to a compact icon (`ic_lock_24dp`/`ic_lock_open_24dp`, tint signals state), freeing space so `tvAppLabel` (app name) renders fuller instead of getting crowded. `AppAdapterWidgetTest`/`FrmAppsWidgetTest`/`FrmAppsLifecycleIntegrationTest` 10/10 pass, lint unchanged. Self-audited **9.7/10** (2026-09-12, TECNO KJ7).
- UI-005 — Dynamic color audit fix: ~30 files (20 vector icons, 6 shape/gradient drawables, 7 layouts, 3 Kotlin/Java call sites) referenced the static `@color/colorPrimary`/`@color/colorAccent` instead of the `?attr/` theme roles `DynamicColors` actually patches; fixed all of them plus a real dark-mode-invisible-icon bug (`ic_language_24dp`, no tint, pure black fill). Also found and fixed a deeper structural gap: two dialog themes (`MaterialYouDialogTheme`, `TransBottomSheetDialog`) were built on non-Material3 parents, so `DynamicColors.applyToActivitiesIfAvailable()` (Activity-only) could never reach them regardless of `?attr` fixes inside their layouts — switched both to their Material3 `ThemeOverlay.*` equivalents. Lint 0 errors/20 warnings (unchanged, no new). Full 119-test instrumentation regression found and fixed one real test bug (`LanguageBottomSheetWidgetTest` inflating against an unthemed context, plus assertions still pinned to the old static color) and one confirmed pre-existing unrelated flake (`SuperWebViewActivityWidgetTest`, passed clean on retry); 0/119 failures after the fix. Self-audited **9.6/10** (2026-09-12, TECNO KJ7 — owner-approved one-off exception to the S24 Ultra policy, second device incident this session after `BUILD-002`/`LINT-004..008`).

- UI-002 — Material You full revamp (all screens): All screens (`ActSettings`, `ActAbout`, `ActVipManagement`, `SuperWebViewActivity`, `SplashAct`), bottom sheets (`LanguageBottomSheetFragment`), dialogs (Sort, Night mode, Highlight color, App folder) migrated to Material You M3 tokens, high-contrast dynamic colors, and edge-to-edge system bar controllers. Legacy `afollestad.material-dialogs` eliminated. 3-tier test suite established (Unit + Widget + Integration), 100% pass on TECNO KJ7. Self-audited **9.8/10** (2026-09-20, TECNO KJ7).
- SEARCH-005 — Contacts search + quick call/message: Zero-persistence ephemeral contact queries (`contact`, `call`, `message`, `lien he`, `goi`), standard `ACTION_DIAL`/`ACTION_SENDTO` intents, single-prompt permission affordance with memory. Connected test runner crash fixed via test hook. Full regression: 327 unit tests, 22/22 widget tests, 2/2 integration tests, 0 lint errors, TECNO KJ7 smoke verified. Self-audited **9.6/10** (2026-09-20, TECNO KJ7).
- ARCH-001 — Decompose complex UI controllers: `ActSettings.java` decomposed from 946 to 531 lines (-415 lines), moving menu routing, dialog coordination, ad state, VIP actions and intent construction into cohesive Kotlin collaborators (`SettingsMenuRouter`, `SettingsDialogCoordinator`, `SettingsAdVipDelegate`, `SettingsIntentHelper`). `onOptionsItemSelected` cyclomatic complexity dropped 20→2, cognitive complexity 128→2. 3-tier test suite added: 5 new unit test classes (367/367 unit tests green), 1 new widget test (3/3 green), 1 new integration test (3/3 green), full regression pass (12/12 connected tests green on TECNO BG6). Self-audited **9.80/10** (2026-09-20, TECNO BG6).
- DOC-001 — Consolidated stale documentation (2026-09-23, owner-picked filler once the real backlog ran dry — everything else chains to declined `ADS-001` or its blocked dependents, or is CI-gate/`store-assets` scope already declined). Made this file the explicit live status source; added a `⚠️ SUPERSEDED` banner (pointing back here) to every dated historical doc still claiming stale "55+ tests"/"0 issues" numbers (`memory_leak.md`, `FIX_SUMMARY.md`, `CODE_REVIEW_RISKS.md`, `UNIT_TEST_SUMMARY.md`, `TESTS_README.md`, `app/src/test/README.md`, `MIGRATION_GUIDE.md`, `AD_SDK_TEST_PLAN.md`); root `README.md` gained a "Docs" section linking to `CLAUDE.md`/this file/ad docs/`store-assets` instead of duplicating content. Live count verified: 434 unit tests, 0 failures (`./gradlew testDevDebugUnitTest`). One item deliberately left open rather than guessed at: `doc/init.md`'s content (currency-input notes) doesn't match this app — flagged for owner review, not deleted. Doc-only change; no code/test-layer touched. Self-audited **9.2/10**.
- FEAT-005 — "Keep screen on" toggle (2026-09-23, owner-picked first `/loop` pick off the fresh backlog): new `UtilSettings.KEY_KEEP_SCREEN_ON` (default off) + `FrmSettings` switch row, following `UI-001`'s `KEY_SHOW_SEARCH_BAR` pattern exactly; `ActHome.updateKeepScreenOnFlag()` re-reads on every `onResume()` (mirrors `updateSearchBarVisibility()`) to set/clear real `FLAG_KEEP_SCREEN_ON` — no explicit `onPause` handling needed since a `Window` flag only governs screen-on state while its window is foregrounded (platform semantics, not assumed). Hint text discloses the battery-saver-can-still-override caveat, translated into all 16 non-English locales. 4 new unit + 4 new widget + 3 new integration tests (real `Window.attributes.flags` bit assertions), all pass; full 425/425 unit regression; lint 0 errors/9 warnings unchanged. Live-verified both directions on Samsung SM-S928B (S24 Ultra): `dumpsys window` `fl=` bit `0x80` toggled on/off correctly via real UI taps, screenshotted. Self-audited **9.6/10**.
- SEARCH-007 — Focus/DND quick toggle (2026-09-23, second `/loop` pick): new `QuickActionEngine.resolveDnd` (`dnd`/`focus`/`khong lam phien`/`tap trung` keywords), `ActHome.openDndAccessSettings()`/`performDndToggle()`. Deliberately diverges from `SEARCH-004`'s flashlight/wifi "remembered decline hides the row" pattern — DND special access has no OS permission popup to avoid re-nagging with, so the row always stays available with denied-state copy instead of vanishing, documented at the point of divergence. 7 new unit tests (Robolectric `ShadowNotificationManager`) + 1 new widget test + 3 new integration tests (real `cmd notification allow_dnd` grant + real `NotificationManager.currentInterruptionFilter` toggle, same shell-grant technique `LensPhysicsPolicyIntegrationTest` already established); full 432/432 unit regression, 23/23 `AppSearchWidgetTest` regression, lint unchanged. Device switched Samsung S24 Ultra → TECNO KJ7 mid-story at owner's explicit request; hit and resolved a real device-state issue along the way (`UiAutomation` shell-command channel needed a device reboot + owner unlock to recover — reproduced against a pre-existing already-shipped test first, confirming it wasn't new-code breakage before troubleshooting). Live-verified on TECNO KJ7: real Settings screen opened, real resume-recheck showed the correct denied-state Vietnamese copy after backing out without granting; the grant→toggle step itself proven by the integration test on the same device (OS notification-access list didn't surface this app within the session — an OEM-side lag, disclosed rather than hidden). Self-audited **9.3/10**.
- SEARCH-008 — QR/barcode scan quick action (2026-09-23, third `/loop` pick): new `QuickActionEngine.resolveScan`, reusing the existing plain `QuickAction.Action` type wholesale — no new sealed subtype, `ActHome` UI branch, or string resource, smallest-footprint of the three quick-action stories shipped this session. Required a new `AndroidManifest.xml` `<queries>` entry for `com.google.zxing.client.android.SCAN` — API 30+ package visibility means `resolveActivity()` always returns null for undeclared actions regardless of whether a matching app is installed, reasoned through proactively rather than discovered by a failure. 5 new unit tests (shadow `PackageManager`, both scanner-present/absent branches) + 1 new widget test + 2 new integration tests (real `PackageManager.resolveActivity()` proving the `<queries>` entry actually works, which Robolectric can't verify); full 437/437 unit regression, 26/26 `AppSearchWidgetTest` regression. Caught and fixed its own `UseKtx` lint regression (`Uri.parse` → `.toUri()`) in the same round rather than shipping it. Live-verified on TECNO KJ7: real tap opened `com.android.vending` (Play Store) via the fallback path (device has no scanner app registered for the `SCAN` action — disclosed, not hidden; the direct-scan branch is proven by the unit test's shadow-registered-resolver case instead). Self-audited **9.5/10**.
- UI-021 — Predictive back gesture (2026-09-23, fourth `/loop` pick): `android:enableOnBackInvokedCallback="true"` app-wide (`tools:targetApi="33"` suppressing the expected minSdk-25 `UnusedAttribute` warning); `ActHome`'s only real `OnBackPressedCallback` (the `UI-010` search-overlay guard) refactored to a named field and given real predictive-back progress hooks (`handleOnBackStarted`/`Progressed`/`Cancelled`) that scale/fade `searchView` (1.0→0.95, 1.0→0.7 alpha) — deliberately built on this app's own already-proven interception point rather than Material `SearchView`'s internal `MaterialBackOrchestrator`, the exact mechanism `UI-010` found unreliable on real hardware. Kept unconditionally enabled (never toggled by `isShowing()`) since disabling it on this HOME-activity task root risks the `finish()` `UI-010` explicitly prevents. 5 new widget tests (real `BackEventCompat` driving the actual production callback via reflection, incl. two UI-010 regression guards), full 437/437 unit regression, lint unchanged. Smoke on TECNO KJ7: real `KEYCODE_BACK` dispatch verified correct; two coordinate-guessed edge-swipe attempts to trigger the literal gesture instead landed on unrelated device content (a long-press menu, then briefly another installed app's photo picker) — backed out immediately with no further screenshots, `navigation_mode` reverted, and the live on-screen swipe animation left honestly unverified rather than faked. Self-audited **9.1/10**.
- FEAT-006 — Export/import launcher layout (2026-09-24, fifth `/loop` pick): `model/LayoutBackup.kt` (versioned JSON, `org.json`, no new dependency) + `util/LayoutImportApplier.kt` (matches by `AppPersistent.generateIdentifier` — component identity, not label — reuses the existing single-app persistence statics in a loop rather than a new bulk query) + SAF (`CreateDocument`/`OpenDocument`) wired through `ActSettings`/`util/LayoutBackupIo.kt` (a Java-to-Kotlin-coroutine callback bridge). Corrected this story's own initial framing along the way: `AppOrganizationRules` is just a folder-name validator, not a separate data holder — `AppPersistent` alone is the real source of truth. 14 new unit tests + 2 new widget tests + 2 new integration tests (real file + real Room round trip); full 451/451 unit regression, lint held at 9 warnings (caught and fixed a self-introduced `PluralsCandidate` regression in the same round). **Live-verified the full export→uninstall→reinstall→import cycle** on TECNO KJ7 against a real 67-app inventory — both preview dialogs ("Ứng dụng được cập nhật: 67. Ứng dụng bị bỏ qua: 0") matched exactly before and after a genuine `adb uninstall`/reinstall, no crash. One real mid-session coordinate-scaling mistake (toggled an unrelated switch) caught and corrected via the next screenshot rather than compounding it. Self-audited **9.6/10**.

- FEAT-007 — Multi-select bulk actions in the Apps tab (2026-09-24, sixth `/loop` pick): `AppAdapter` gains selection state (`Set<String>` of `AppPersistent.generateIdentifier`-style identities) rendered through a manual `notifyItemChanged`/`notifyItemRangeChanged(..., PAYLOAD_SELECTION)` path, a real `ActionMode` (`FrmApps` as `ActionMode.Callback`), and bulk hide/unhide/pin (`AppAdapter.bulkSetVisibility`/`bulkPin`, looping the exact single-app `AppPersistent` codepath) plus a deduped-by-package bulk uninstall confirmation. Selection UI reuses `MaterialCardView`'s native `checkable`/`checkedIcon`/`strokeColor` — no hand-rolled overlay. **Two real defects found live and fixed in the same round, not just assumed away**: (1) the pre-existing drag-reorder `ItemTouchHelper.isLongPressDragEnabled()=true` silently swallowed every long-press before it could reach the new selection listener — confirmed by long-press doing *nothing* on a real device, fixed by disabling it (reordering stays available via the existing "Move earlier"/"Move later" menu actions); (2) `ActSettings`'s `viewpager.setOffscreenPageLimit(2)` (3 tabs total) keeps every tab's fragment view — and `Fragment.onPause()` — alive/never-fired across ordinary tab switches, so the initial assumption that view teardown would clear selection was wrong; caught by a new integration test written directly against the acceptance wording, fixed with `FrmApps` registering its own `ViewPager2.OnPageChangeCallback`. 8 new pure unit tests (no Context) + 3 new widget tests + 5 new integration tests (real `ActionMode`, real Room persistence + `RAppsSingleton` reflection, the tab-switch regression test); full 459/459 unit regression; full connected regression on both devices shows every `AppAdapter`/`FrmApps`/`AppOrganization` test (45 total) green, remaining failures pre-existing and code-unrelated (DND/search-debounce timing, a locale space-formatting quirk affecting unrelated tests identically on both devices — one of which was itself traced to a mid-run screen-lock on Pixel 7 Pro, confirmed via `dumpsys power` and resolved by keeping the screen awake, not a code fix); lint held at 9 warnings. **Live-verified the full select→ActionMode-content→hide→unhide→pin→uninstall-confirm→cancel flow** on TECNO KJ7 against real installed third-party apps, device restored to its original state afterward. One real mid-session coordinate-scaling mistake (an un-scaled screenshot coordinate collapsed a 2-item selection back to zero) caught and corrected the same way `FEAT-006` disclosed its own. Self-audited **9.5/10**.
  - **Audit round 2** (owner-requested re-audit, same day): found and fixed **3 more real defects** the round-1 self-audit missed — (1) deselecting the last selected item left that row's hide/lock/menu icons permanently hidden (`setSelectionState()` never restored them on exit, only ever hid them on entry); (2) a background app-list refresh (`updateApps()`) never pruned the selection, so a removed app stayed "selected" forever; (3) owner flagged the contextual action bar as visually inconsistent ("status bar tint + navigation bar tint + color icon + color label sai tùm lum") — the CAB had never been themed (first `ActionMode` this app has ever shown) and the selection indicator used a stroke + saturated color against the owner's stated pastel/no-stroke preference; fixed by theming `actionModeStyle` to the toolbar's own `colorPrimary`/`colorOnPrimary` tokens and switching the card indicator to a stroke-free `colorPrimaryContainer`/`colorOnPrimaryContainer` pastel fill. All three reproduced by a test (or the owner's own eyes) failing before the fix; 8 new tests added (3 widget, 5 integration, closing every remaining "direct adapter call vs. real `ActionMode` menu click" gap); full 459/459 unit + 32/32 targeted instrumentation green on two additional real devices (TECNO BG6, OPPO CPH1989) with genuinely different Material You dynamic palettes, confirming the color fix generalizes; lint unchanged. Re-audited **9.3/10** — the second-pass-finds-3-more-bugs outcome is itself the honest signal that round 1's "9.5" measured confidence, not defect-free-ness. Committed locally (`41aacbd`); `git push origin dev` failed with a real permission error (`403`, "Permission to tplloi/lens-launcher.git denied to gj-loitp") — not yet pushed, owner needs to resolve push access (repo moved to `royt93/5_lens-launcher` per this session's own memory) before this reaches the remote.

- UI-022 — Gesture shortcuts directly on the lens grid (2026-09-25, seventh `/loop` pick): offered the owner a choice between the ticket's original "long-press-then-directional-swipe" design and a simpler "long-press-and-hold opens a menu" design (`AskUserQuestion`) given the real pan-gesture-conflict risk the ticket's own context section named; owner picked the simpler one. Long-press on `LensView` now reuses `R.menu.menu_search_result` and `SearchResultAdapter`'s exact info/pin/uninstall intents and organization-persistence logic verbatim (SEARCH-003) via a new `LensView.showQuickActionsMenu()`, wired as the single shared entry point for both the touch gesture and TalkBack's long-click action (`showAppOptionsAtIndex()`, previously info-only). Gesture disambiguation is an explicit state machine (`mLongPressArmed`/`mMoving`/`mLongPressTriggered` + two pure, unit-tested predicates), zero changes to `LensGridCache`/`drawGrid` (`PERF-001`'s hot path untouched). **Two real defects found and fixed within this same round**: (1) `PopupMenu` imported from `android.widget` instead of `androidx.appcompat.widget` — compiled fine but `setForceShowIcon()` requires API 29 against this app's minSdk 25, caught by `lintDevDebug` itself (`NewApi`, 1 error) before reaching a device; (2) `popupMenu.inflate()` threw on a non-Activity context with no Material3 theme — every pre-existing `LensView` widget test happened to construct the view that way since none had ever shown themed UI before, so a new real-`ActHome`-Activity integration test was needed specifically to prove genuine success rather than a bare-view test swallowing the same crash silently; fixed by wrapping the whole construct-inflate-show sequence in one `try`/`catch` and adding that integration test. 11 new pure unit tests + 5 new widget/a11y tests + 2 new integration tests (real `ActHome` via `ActivityScenario`, both the touch and TalkBack paths independently proven to actually succeed on a real attached+themed window); existing `LensGridCacheTest`/`LensPhysicsPolicyTest`/`LensViewInsetsRegressionWidgetTest`/`LensViewIconReloadWidgetTest`/`AdaptiveMultiWindowIntegrationTest`/`SearchResultAdapterWidgetTest` all re-run and green; lint held at 0 errors/9 warnings after fixing the `NewApi` regression this story itself introduced and caught. **Live-verified on TECNO KJ7**: long-press → real quick-actions popup → tapped App Info → real system App Info screen opened for the exact app; a real drag/pan afterward was completely unaffected (no spurious menu, no crash); repeated on a second, different icon for consistency. Disclosed cosmetic limitation: the menu opens at a fixed position relative to the whole `LensView`, not anchored to the pressed icon's own rect (no per-icon child View exists to anchor to; a transient-anchor-View fix is possible but non-trivial for a purely cosmetic gain). Self-audited **9.3/10**. Committed locally; not yet pushed (same pending push-access issue as FEAT-007's round 2, above).

- PERF-004 — Baseline Profile for cold start (2026-09-25, owner-picked as step 1 of a 4-item batch): new `:baselineprofile` module (`BaselineProfileGenerator` + `ColdStartupBenchmark`/`StartupTimingMetric`, explicit-component launch of `ActHome` since `CATEGORY_LAUNCHER` resolves to `ActSettings`), generated profile committed under `app/src/main/generated/baselineProfiles/` (8,916 rules) and shipped as `assets/dexopt/baseline.prof`, `profileinstaller` pinned explicitly. Found and fixed two gaps the ticket didn't name: `reportFullyDrawn()` was never called (now fired once, gated by pure `UtilCalculator.shouldReportFullyDrawn`, when icons first reach `LensView`), and the release-signing guard's regex also matched the plugin's `benchmarkRelease`/`nonMinifiedRelease` tasks (narrowed to exclude only those; real release tasks still fail without signing, proven by a new contract test). Benchmark variants get `IS_ENABLE_ADMOB=false`. Pixel 7 Pro medians: `timeToInitialDisplay` 303.0→285.9 ms, `timeToFullDisplay` 2,337.5→2,255.3 ms (dominated by the async package scan), `am start -W` 205→187 ms; `dumpsys` shows `speed-profile`. 4 unit + 1 contract + 2 widget + 1 integration + 2 Macrobenchmark tests; 477/477 unit, lint unchanged, `ui` package 137/139 (2 pre-existing locale-space failures). Self-audited **9.25/10**.

## 🟡 In progress

- SEC-001 — Local signing remediation is complete. Play Console rotation/revocation, CI secret replacement, non-production upload validation and coordinated Git-history cleanup require publisher-owner access.
- ADS-001 — Consent-driven advertising state machine handed to the Ad SDK team (2026-09-06); excluded from this repo's code-loop until they deliver. VIP-001 stays blocked on this dependency.

## 📋 Picked

| Order | Story | Priority | SP |
|---:|---|:---:|---:|
| 1 | VIP-001 Replace reusable VIP secrets | P1 | 13 → split required, blocked on ADS-001 |
| 2 | STORE-001 Harden store-assets write/upload APIs | P1 | 8 |
| 3 | STORE-002 Add revision-safe project persistence | P1 | 5 |
| 4 | REL-002 Add Play/privacy release gate | P1 | 8 |
| 5 | TEST-001 Establish trustworthy CI test gates | P1 | 8 |
| 6 | TEST-002 Build complete test coverage and Tecno smoke matrix | P1 | 8 |
| 7 | AUDIT-001 Score every change round and gate push | P1 | 3 |
| 9 | FISH-006 Smart Focus, lite (no new data collection) | P2 | 8 |
| 10 | UI-022 Gesture shortcuts on the lens grid | P2 | 5 |
| 11 | FEAT-007 Multi-select bulk actions in the Apps tab | P2 | 5 |
| 12 | FISH-007 Depth-of-field blur by focus distance | P2 | 5 |
| 13 | FISH-009 Live pinch-to-adjust lens curvature | P2 | 5 |
| 14 | FISH-008 Multi-lens workspaces | P2 | 13 → split required |

## ⏸️ Deferred

- Remaining P2/P3 engineering improvements stay deferred until the next owner selection.
- Fisheye Smart implementation starts after baseline frame-time and accessibility measurements exist.

## ❌ Skipped

- `store-assets` implementation work is excluded from the current product loop by owner decision. Reopen its security/store stories before deploying that tool.
- `FISH-002` (Edge alphabet Fisheye scrubber) declined by owner (2026-09-20); remains in `todo` as historical backlog record.

## 💭 Ideas

- Accessible list mode, large-screen support, store asset automation, local insights, privacy-first monetization, and five Fisheye Smart concepts remain captured as individual `idea` stories in `todo`.
- `UI-021`/`PERF-004`/`FEAT-005` (2026-09-23, owner-picked from a ponytail-audit-triggered perf/Material You gap check): predictive back gesture, Baseline Profile cold-start, and a "keep screen on" toggle — scoped as full stories, see `todo/`. `FEAT-005` and `UI-021` shipped the same day (see Implemented); `PERF-004` remains in `todo/`.
- Not yet scoped into a story (flagged, not picked): themed/monochrome adaptive app icon (Android 13+, `<monochrome>` missing from `mipmap-anydpi-v26/ic_launcher.xml` — ties into the existing dynamic-color work from `UI-005`); `StrictMode` in debug builds to catch main-thread I/O/leaked closeables during development (dev-time only, zero user-facing risk).
- `FISH-006`/`FEAT-006`/`UI-022` (2026-09-23, owner-picked exclusive-feature brainstorm, deliberately scoped to avoid the declined `ADS-001`/`INSIGHT-001`/`VIP-001`/`REL-002` chain): a lite re-scope of `FISH-001`'s Smart Focus using only the `open count` Room already persists (no new tracking), export/import of the existing layout data model, and lens-grid gesture shortcuts reusing `SEARCH-003`'s already-extracted intent helpers. `FISH-006` is explicitly a re-scope proposal for owner sign-off, not an automatic go — see the story file.
- `SEARCH-007`/`SEARCH-008`/`FEAT-007` (2026-09-23, same brainstorm, round 2): DND/Focus quick toggle and QR-scan delegation follow the exact `QuickActionEngine`/`SEARCH-006` delegate-to-installed-app pattern (zero new dependency); multi-select bulk actions in the Apps tab uses native `ActionMode`, addressing the large-app-count pain point `UI-020` already documented live (346 apps on one owner device).
- `FISH-007`/`FISH-008`/`FISH-009` (2026-09-23, round 3 — owner explicitly asked for flagship/exclusive ideas tied to the "fisheye lens" brand itself, not generic utility): depth-of-field blur by focus distance, multi-lens workspaces (swipe between independently-configured lenses — the strongest differentiator of the three, and the riskiest; 13 SP, explicitly flagged needing a pre-implementation split per the working agreement), and live pinch-to-adjust lens curvature. All three touch `LensView`/`LensGridCache`, this codebase's highest-risk hot path (`PERF-001`'s own characterization) — each story's own Acceptance criteria and Loop end condition call this out explicitly, including the specific evidence (real-device frame timing, gesture-conflict integration tests) an audit round should require before scoring `> 9.0`.
- ✅ Material You full revamp (owner decision, 2026-09-12): scoped and split into `UI-002`/`UI-003`/`UI-004`/`UI-006`, all shipped this session (see Implemented). `UI-005` (dynamic color audit, found along the way) also shipped.
- Search expansion (owner decision, 2026-09-12): scoped and split into `SEARCH-001`/`SEARCH-002`/`SEARCH-003`/`SEARCH-004`/`SEARCH-006` (**all shipped**, see Implemented). `SEARCH-005` (contacts — `READ_CONTACTS`) is now `inprogress` after owner dev-loop sign-off on 2026-09-13; Pixel 7 Pro verification is recorded under the owner-approved S24 replacement exception, while Play Console/privacy-policy contacts declarations remain required before production release. `SEARCH-004`'s own CAMERA/location Play Console Data Safety form update is likewise still owner-pending before a production release (code shipped to dev regardless).
- ✅ Icon size default now auto-scales to device (`smallestScreenWidthDp`) instead of a fixed 18dp — `UtilSettings.calculateAutoDefaultIconSize`, live-verified on TECNO KJ7 (showed 18dp, matching this device's ~360dp baseline).

## Recommended delivery waves

1. **Wave 0 — Incident response:** SEC-001, SEC-002, REL-001.
2. **Wave 1 — Trust and correctness:** ADS-001, VIP-001 split, CORE-001, DB-001, CORE-002. SEC-003 complete.
3. **Wave 2 — Release system:** STORE-001/002, LAUNCH-001, REL-002, TEST-001.
4. **Wave 3 — Quality:** accessibility, performance, lifecycle, preferences, architecture, build reproducibility.
5. **Wave 4 — Product:** FEAT-002, CORE-001 and DB-001 are complete; select the next idea using measured retention and performance.

## Test-layer rule inherited by every story

Every story file inherits TEST-002 even when its local verification section highlights only the most important checks:

- **Unit:** pure logic, parsing, state, validation and deterministic failure branches.
- **Widget/UI:** visible behavior, accessibility, lifecycle and user feedback.
- **Integration:** boundaries across persistence, Android components, SDKs, filesystem/API and process recreation.
- **Tecno smoke:** exact candidate build on the designated physical device, with device/build/network/log evidence.
- A layer may be marked `Not applicable` only with a concrete, reviewed reason in the story's audit record.

## Audit score and push gate

Score each round from evidence, never from task completion claims:

| Dimension | Weight |
|---|---:|
| Correctness and acceptance criteria | 2.0 |
| Unit-test quality and coverage | 1.5 |
| Widget/UI-test quality and coverage | 1.0 |
| Integration-test quality and coverage | 1.5 |
| Tecno + general smoke results | 1.0 |
| Security/privacy/Play readiness | 1.0 |
| Performance, lifecycle and regression risk | 1.0 |
| Maintainability and documentation truth | 1.0 |

Required push predicate: score `> 9.0/10`, zero failed code-controlled checks, a clean secret scan of the proposed diff, and a reviewed audit record. A score of exactly `9.0` does not qualify. External P0/P1 actions require named ownership and must remain visible under `inprogress`; pre-existing repository-wide secrets remain release blockers under SEC-001 and may not be copied, modified or newly exposed by another wave.
